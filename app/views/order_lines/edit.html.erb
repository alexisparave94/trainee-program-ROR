<div class='d-flex flex-column align-items-center'>
  <h2>Edit Line</h2>
  <div class="card p-2" style="width: 20rem;">
    <div class="card-body">
      <h5 class="card-title">Product: <%= @product.name %></h5>
      <h6 class="card-subtitle mb-2 text-muted"><%= format('$ %0.02f', @product.price) %></h6>
      <p class="card-text"><%= @product.description %></p>
      <%= form_with model: @order_line, url: order_line_path(id: @product.id), method: :patch do |form| %>
        <%= form.hidden_field :price, value: @product.price %>
        <%= form.hidden_field :product_id, value: @product.id %>
        <div class="field mb-3">
          <%= form.label :quantity, class:'form-label' %>
          <%= form.text_field :quantity, class: 'form-control' %>
          <div class='mt-3'>
            <% unless @order_line.errors.messages_for(:quantity).empty? %>
              <% @order_line.errors.messages_for(:quantity).each do |message| %>
                <p class='text-danger fw-semibold'>* <%= message %></p>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="d-grid mb-3">
          <%= form.submit 'Update line', class: 'btn btn-primary' %>
        </div>
      <% end %>
    </div>
  </div>
</div>